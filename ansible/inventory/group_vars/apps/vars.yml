---
# apps グループ変数

scanopy_public_url: "https://scanopy.lab.kanare.dev"

ufw_allowed_ports:
  - { port: 22,    proto: tcp }
  - { port: 3001,  proto: tcp }                              # Homepage (Caddy from vm-infra)
  - { port: 9100,  proto: tcp }                              # node_exporter
  - { port: 60072, proto: tcp, from: "192.168.11.0/24" }    # Scanopy UI (LAN only)
